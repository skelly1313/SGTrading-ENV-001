# backtests/

Per-run backtest summaries for `moving-average-crossover`.

Filename convention: `<YYYY-MM-DD>-<short-note>.md`.

Each summary includes: parameter set, sample period, equity curve link (in `DATA/backtests/`), key metrics (CAGR, Sharpe, max DD, hit rate), and conclusions.

Nothing here yet.
